# 环境变量配置

## 开发环境 (.env.development)
```
VITE_SENTRY_DSN=
```

## 生产环境 (.env.production)
```
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
```

## 可选配置
- `VITE_API_BASE`: API 基础 URL（默认：/api）
- `VITE_APP_VERSION`: 应用版本（自动从 package.json 读取）

## 获取 Sentry DSN
1. 访问 https://sentry.io
2. 创建新项目（选择 React）
3. 复制 DSN 到 .env.production
