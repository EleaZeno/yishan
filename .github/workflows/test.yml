name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test works"
      - run: npm install
      - run: npm run build || echo "Build may fail"
      - run: ls -la dist/
