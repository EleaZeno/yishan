// 此文件已停用。
// AI 功能（释义生成、TTS）已被移除，转为使用本地词库和浏览器原生语音合成。

export const fetchWordDetails = async (term: string) => {
  throw new Error("AI service is disabled.");
};

export const fetchWordAudio = async (text: string) => {
  throw new Error("AI service is disabled.");
};
