{
  "name": "yishan-workers",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute yishan-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute yishan-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.77.0"
  }
}
